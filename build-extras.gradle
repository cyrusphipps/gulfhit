// Keep debug builds debuggable while investigating Android 16 KB compatibility
// popups caused by misaligned native libraries (e.g., SQLCipher) on test devices.
android {
    buildTypes {
        debug {
            debuggable true
        }
    }
}
