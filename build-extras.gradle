// Keep debug builds non-debuggable to avoid Android 16 KB compatibility popups
// caused by misaligned native libraries (e.g., SQLCipher) on test devices.
android {
    buildTypes {
        debug {
            debuggable false
        }
    }
}
